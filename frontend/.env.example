# Leave unset for local development: Vite's dev server proxies /api to
# http://localhost:8000 (see vite.config.ts), so requests stay relative.
#
# For a deployed frontend, set this to the deployed backend's absolute URL.
# VITE_API_BASE_URL=https://your-backend.onrender.com
